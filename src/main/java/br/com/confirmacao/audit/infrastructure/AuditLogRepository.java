package br.com.confirmacao.audit.infrastructure;import br.com.confirmacao.audit.domain.*;import org.springframework.data.jpa.repository.JpaRepository;import java.time.Instant;import java.util.UUID;public interface AuditLogRepository extends JpaRepository<AuditLog,UUID>{long countByActionAndCreatedAtGreaterThanEqual(AuditAction action,Instant since);}
